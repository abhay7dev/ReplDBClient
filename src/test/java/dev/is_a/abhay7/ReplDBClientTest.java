package dev.is_a.abhay7;

import org.junit.Test;
import static org.junit.Assert.*;

public class ReplDBClientTest {
	/* private ReplDBClient db;

	@Before
	public void setUp() {
		db = new ReplDBClient();
	}

	@After
	public tearDown() {
		db.empty();
		db = null;
	}

	@Test
	public void settingGettingAndRemoving() {
		String key = "testKey";
		String value = "testValue";
		db.set(key);
		assertEquals(db.get(key), value);
	}

	@Test(expected=IllegalArgumentException.class)
	public void testIllegalUrl() {
		db = new ReplDBClient("abhay7");
	} */

	/* private ReplDBClient db;

	@BeforeEach
	public void beforeEach() {
		db = new ReplDBClient();
	}

	@Test
	public voidTestCached() {
		assertEquals(db.isCached(), true);
	}

	@Test
	public voidTestEncoded() {
		assertEquals(db.isEncoded(), true);
	}

	@Test
	public voidTestDebug() {
		assertEquals(db.isCached(), true);
	} */

}